<!DOCTYPE html>
<html lang="de">
    <head>
        {% metagen 
            title=title,
            desc=meta_desc,
            locale=locale,
            comments=false
        %}
        {% set css %}
            {% include "css/style.css" %}
        {% endset %}
        <style>
            {{ css | cssmin | safe }}
        </style>
    </head>
    <body>
        {% include "partials/header.njk" %}

        {{ content | safe }}

        {% include "partials/footer.njk" %}
    </body>
</html>